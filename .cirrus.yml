task:
  container:
    image: gcc:latest
  build_script:
    - sh build.sh --auto-iar --auto-umber --kos --devices
  amd64_kos_artifacts:
    path: "bin/kos"
  amd64_device_artifacts:
    path: "bin/devices/*"

task:
  arm_container:
    image: gcc:latest
  build_script:
    - sh build.sh --auto-iar --auto-umber --kos --devices
  arm64_kos_artifacts:
    path: "bin/kos"
  arm64_device_artifacts:
    path: "bin/devices/*"
